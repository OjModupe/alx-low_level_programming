#include "main.h"

/**
 * print_last_digit- Prints the last digit numbers of a number.
 * @n: the number to be treated
 * Return: value of the last digit number
 */

 int print_last_digit(int);

 {
	int last;
	
	last = n % 10;
	if (last < 0)
	{
	last = last * -1;
	}
	_putchar('0' + r);
	return (last);
}
